# Git & GitHub Assessment

**Name:** Bindu Priya  

---

## Question 1: Project Initialization & First Push
- Created project folder
- Initialized Git using `git init`
- Created `app.py`
- Checked status using `git status`
- Staged using `git add`
- Committed using `git commit`
- Added remote repository
- Verified remote using `git remote -v`
- Pushed code to GitHub

---

## Question 2: Working with Changes & History
- Modified `app.py`
- Checked changes using `git status`
- Viewed differences using `git diff`
- Staged and committed changes
- Viewed history using:
  - `git log`
  - `git log --oneline`

---

## Question 3: Branching & Feature Development
- Created branch `feature-update`
- Switched branches
- Added feature logic
- Committed changes
- Merged branch into `master`
- Deleted branch

---

## Question 4: Handling Errors (Stash, Reset, Revert)
- Used `git stash` for temporary changes
- Applied stash using `git stash apply`
- Used `git reset` to undo commit
- Used `git revert` to safely reverse commit

---


## ConclusiThis project demonstrates Git operations including initialization, branching, merging, stashing, resetting, and revertin
